import json
import time
import datetime

class DateRangeBuilder:
    @staticmethod
    def build(minUnixDate:int,maxUnixDate:int):
        """
        .build

        @params minUnixDate -> the minimum date of the sample
        @params maxUnixDate -> the maximum date of the sample

        This method returns a array of dates , following a 
        standarized normal ((0,1)) distrubution with min -> minUnixDate and 
        
        max-> maxUnixDate. 
        """
        pass
    
